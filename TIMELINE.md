# 마스터 타임라인

기록 규칙: 의미 있는 결정·발견·실행은 날짜와 함께 여기 남긴다. 사후 수정 시 사유를 적는다.

## 2026-08-20
- H-UP 12기 지원서 제출. 팀 종민재(이종민·정민재), 지도교수 공형우.

## 2026-08-28
- 「부실 사건 정의 및 데이터 설계 v0.1」 작성. 1차 면담 논의용.
- 주 정의를 감사의견 비적정 + 회생절차로 한정. 관리종목은 보조 정의로 분리.
- 기준시점을 결산일이 아니라 DART 접수일자로 결정.

## 2026-09-01
- 서약서 제출. 2학기 시작, W01 개시.

## 2026-09-02
- 프로젝트 저장소 구성 (`~/Desktop/h-up/project`).
- 수집·라벨·피처·데이터셋·모델·설명 파이프라인 구현. 스모크 테스트 5건 통과.
- **발견 A-1**: 감사의견은 해당 사업보고서 안에 있다. 같은 보고서로 그 의견을 예측하면
  동어반복. → 사건일 > 기준시점을 등호 없이 강제. `test_label_timing` 으로 고정.
- **발견 A-2**: 예측 창 365일이면 사업보고서 제출 간격 368일짜리 사건이 통째로 누락된다.
  → 365일 주 분석 + 400/450일 민감도 병행으로 결정.
- **발견 A-3**: 시점 분할과 기업 단위 그룹 분할을 동시에 걸면 실제 운영 환경과 달라진다.
  → 시점 분할을 주 설계로, 그룹 분할은 보조 분석으로 강등.
- **발견 A-4**: 감사의견을 `accnutAdtorNmNdAdtOpinion.json` 으로 정형 취득 가능.
  v0.1 주의 2(본문 파싱 부담)가 크게 완화됨. 단 커버리지는 W03에 실측할 것.
- 검증 도구 4종 작성 (섭외 메일 / 블라인드 평가지 / 인터뷰 가이드 / 갭 분석 시트).
- W01~W15 주차 문서와 블로그 초안 15편 작성.

## 2026-09-02 (오후)
- **범위 변경**: 실무자 인터뷰·블라인드 평가를 이번 진행에서 제외.
  W05(섭외) → 계정 매핑 검증 / W11 → 설명 충실도 정량 검증(삭제 검사) /
  W12 → 오분류 사례 분석 / W13 → 사건 기업 공시 원문 기반 정보 목록화.
  `eval/` 4종은 삭제하지 않고 보류 표시만 붙임.
  → 지원서의 핵심 산출물이 바뀌므로 지도교수·프로그램 측 통지 필요.
- 파이프라인 전 구간 연결 (`pipeline.py`): 캐시 → 패널 → 라벨 → 학습 → 설명 카드.
  `fnlttSinglAcntAll` 응답의 `rcept_no` 앞 8자리가 접수일자라 공시검색 2,500회를 아꼈다.
- 합성 데이터로 전 구간 예행 (`tests/test_pipeline.py`). 키가 오는 날 첫 실행을 하지 않기 위해서.
- **버그 2건 수정**
  1. `apply_clean` 이 구간마다 결측 더미 열을 따로 만들어 검증·평가에서 KeyError.
     → 더미 열 목록을 `fit_clean` 이 학습 구간에서 한 번 정한다.
  2. 설명 카드 방향이 뒤집혀 "단기 지급능력에 여유가 있습니다"가 *위험을 높인 요인*에 붙었다.
     → 묶음은 SHAP 부호, **문장은 관측값**으로 분리. 변수별 위험 방향을 `HIGH_IS_RISKY` 로 명시.
     부작용으로 "매출이 줄었는데 위험을 낮춘 요인"이 그대로 노출되는데, 이건 감추지 않는다.
     모델이 이상하게 판단한 지점이고 W11~W12의 재료다.

## 2026-09-02 (저녁) — 1주차 산출물

- **선행연구 5편 검토** (`docs/02_선행연구_검토.md`): Beaver 1966 / Altman 1968 /
  Ohlson 1980 / Zmijewski 1984 / Shumway 2001. 요약이 아니라 우리 설계 점검표로 정리.
  - A-1·A-2는 새 발견이 아니라 **Ohlson(1980)이 이미 다룬 함정**임을 확인.
  - "매칭 학습 + 전수 평가"의 근거가 **Zmijewski(1984) 선택 기반 표본 편의**임을 확인.
  - **신규 항목 발생**: Shumway(2001) — 기업-연도 패널의 반복 관측은 독립이 아니므로
    계수가 과대 유의해 보인다. → **7주차에 기업 단위 군집 표준오차 추가**.
  - **한계 확정**: 시장 변수(시가총액·과거 수익률·변동성)를 배제한 것이 구조적 한계.
    Shumway는 이쪽이 재무비율보다 낫다고 본다. 보고서 한계 절에 명시 + 비교모형 검토.
- **1차 면담 자료** 1장 작성 (`docs/03_1차면담_자료.md`) — 확정 안건 5개 + 범위 변경 보고.
- 팀 역할 분담 초안 작성 (weeks/W01). 정민재 확인 대기.
- 국내 신용평가 방법론은 **미확인**. 자료 목록만 두고 면담 후 읽는다.

## 2026-09-02 (밤) — 시장 변수 비교모형

- 1주차 검토에서 "한계로 명시"까지만 하려던 Shumway 시장 변수를 **실험으로 전환**.
  `src/hup/market.py` + `pipeline.compare()`: 재무만 / 시장만 / 재무+시장을
  같은 분할·같은 모델·같은 정제 기준으로 비교한다.
- **수집 경로 확인**: pykrx에서 `get_market_ohlcv`(일별 시세)만 살아 있다.
  시가총액·지수·종목목록 엔드포인트는 빈 응답. 그래서
  시가총액 = 종가 × 발행주식총수(DART `stockTotqySttus`),
  시장수익률 = 표본 동일가중 자체 구성. **지수 미사용은 한계로 명시.**
- `test_market_vars_ignore_the_future`: T 이후 가격을 50배로 조작해도 T 시점
  변수가 안 변하는지 확인. 시장 변수는 미래 정보가 새기 가장 쉬운 지점이다.
- 결과 해석 기준을 **8주차 문서에 미리 적어뒀다.** 숫자를 보고 나서 해석을 정하면
  원하는 결론이 나온다.

## 2026-09-03 — DART 키 수령, 실데이터로 버그 4건 발견

키 발급 확인. 실제 응답을 보자마자 합성 데이터로는 안 잡히던 문제들이 나왔다.

1. **감사의견 응답은 3행이다** — 당기·전기·전전기가 같은 `rcept_no` 로 온다.
   전부 사건으로 세면 과거 의견이 나중 보고서 접수일에 다시 잡히는 유령 사건이 생긴다.
   → `labels.current_period()` 로 당기만. 실제 서식을 픽스처로 굳혀 테스트에 넣음.
2. **계정 매핑이 재무제표 구분을 안 봤다** — 매출채권 성공률 39.8%.
   값이 비는 것보다 나쁜 건, 현금흐름표의 '매출채권의감소(증가)'와 장기매출채권이
   유동 매출채권 자리에 조용히 들어올 수 있었다는 것.
   → BS/IS·CIS/CF 구분 + 후보 ID 복수화. **39.8% → 96.9%**.
3. **`list.json` 은 corp_code 없이 3개월 제한** (status 100).
   `labels.rehab_events` 가 11년 구간을 통으로 불러서 실행 즉시 터졌을 것.
   → `disclosures_all` 이 분기로 잘라 돈다. 두 호출자 동시 수정.
4. **접수연도 ≠ 사업연도** — `report_nm` 이 '사업보고서 (2023.12)'. 2024년 3월 접수분은 FY2023.
   접수연도를 사업연도로 쓰면 **데이터셋이 통째로 한 해 밀린다.** 수집 시작 직전에 잡았다.
   → `annual_filers` 가 report_nm 에서 (사업연도, 결산월)을 뽑는다.
      정정·첨부추가본이 여러 건 오므로 **최초 접수일**을 기준시점으로 쓴다.

**수집 대상 축소**: corp_codes 3,988 → 상장 & 실제 제출 기준. 빈 응답 호출이 사라진다.

**뜻밖의 소득**: 감사의견 응답에 `emphs_matter`(강조사항), `core_adt_matter`(핵심감사사항),
`adt_reprt_spcmnt_matter` 가 함께 온다. 12~13주차에 원문을 읽으려던 것이 정형으로,
추가 호출 없이 온다. → `labels.audit_matters()` 추가. W13 을 표본이 아니라 **전수** 분석으로 격상.
변수 1개 추가 후보를 `계속기업불확실성`으로 확정(단 FY t-1 값만 사용 — A-1 과 같은 함정).

**사고 1건**: `str.replace("", new)` 로 `dart.py` 를 9.7MB 로 파손. 새로 작성해 복구.
인덱스 슬라이스로 만든 문자열을 검증 없이 replace 에 넣지 말 것.

## 2026-09-03 (계속) — 본수집 시작

- `python -m hup.cli fs` 백그라운드 실행. 대상 3,412사 / 기업-연도 28,307건 / 예상 호출 56,614건.
  일 20,000건 한도라 1개 키로 3일. 한도 도달 시 자동 중단, 캐시는 남는다.
- 사업연도별 기업 수 2,216(2015) → 2,924(2025). 상장사 증가 추세와 일치.
  결산월 12월 27,645 / 3월 263 / 6월 215 / 9월 75 / 11월 27 → 비12월결산 약 650건 제외 예정.
- **버린 395건을 확인했더니 394건이 「사업보고서제출기한연장신고서」였다.**
  파서는 옳게 걸렀지만 버릴 데이터가 아니었다. 298개사 / 전체의 1.39%.
  → `dart.extension_filers()` 추가 (캐시 사용, 추가 호출 0건).
  → 패널에 `제출기한연장` 컬럼으로 적재. 시점 문제 없음(연장신고가 보고서보다 먼저 접수).
  → 기본 모델에는 넣지 않는다(계획서 범위는 재무제표 기반). 13주차 추가 변수 후보.
  → **4주차 A-2 진단에 투입**: `pipeline.label_loss_by_extension()`.
     12개월 창에서 놓치는 사건이 연장 기업에 쏠려 있는지 잰다.
     쏠려 있으면 라벨 손실이 무작위가 아니라는 증거가 된다.

## 2026-09-03 (계속) — `last_reprt_at` 함정, 2키 병렬 전환

- **회생절차 수집 검증 중 사건 누락 발견.** `list.json` 의 `last_reprt_at=Y`(기본으로 쓰던 값)는
  최종보고서만 남겨서 **원본 공시를 지운다.** 2023년 1분기 회생절차 6건 중 1건(디엘팜)이 사라졌다.
  → 기본값을 `N` 으로 변경. 중복은 호출부에서 min(rcept_dt) 로 정리. docs/01 부록 A-6.
  → 같은 이유로 사업보고서 기준시점 T 도 정정본 날짜로 밀려 있었다. universe 재빌드.
- 정민재 학우 키 확보. `cli.fs [샤드/전체]` 로 인터리브 분할 추가.
  corp_code 순 반분할이 아니라 `i % 2` 인 이유: 오래된 기업일수록 연도 수가 많아
  앞쪽 절반에 작업이 몰린다. 캐시는 crtfc_key 를 키에 넣지 않아 두 프로세스가 공유한다.

## 2026-09-03 (저녁) — 실데이터 라벨 검증

수집 도중, **API 호출 0건으로**(캐시된 감사의견만 사용) 라벨 경로를 완주시켰다.

- 감사의견 전 연도 캐시 1,793사 × 11년 → **비적정 353건 = 1.79%**.
  계획서 예상(1.5~2.5%)과 일치. 라벨 경로가 실제로 동작한다.
- **버그 9**: `adt_opinion` 이 자유 텍스트라 `거절` 단독 표기 18건을 놓치고 있었다.
  `부적정`이 `적정`을 부분문자열로 포함하는 함정도 있어 판정 순서를 못박았다.
- **버그 10**: 감사의견 빈 값 5.6%(1,175건)를 '비적정 아님 = 정상'으로 처리하고 있었다.
  모르는 것을 정상이라고 라벨링한 것. `classify_opinion()` 이 `불명`으로 분리.
  4주차에 민감도(정상 처리 vs 제외)로 다룬다.
- **버그 7 (연결 전용 조회)** 수정 효과: 별도(OFS) 응답 4,886건 확보.
  이전 코드에서는 전부 사라졌을 관측치다.
- 인프라: DNS 단절로 3개 프로세스가 동시에 죽어 `run_collect.sh` 감시 루프 도입.
  파이썬 내부 재시도(31초)를 넘는 장애는 바깥에서 되살리는 게 맞다.

## 2026-09-04 — 수집 완료, 라벨 전 구간 첫 완주

수집 100% (3,412사 / 28,314 기업-연도). `dryrun` 이 처음으로 끝까지 통과했다.

```
패널 24,965행   부채비율 결측 아님 99.6%
제외 1,881행 → 금융업 1,135 / 스팩·리츠 416 / 비12월결산 330
사건 1,027건 (감사의견 673 / 회생절차 354)
분할  train 13,547(사건 204) / valid 2,235(34) / test 4,805(92)
```

- **버그 11**: `list.json` 페이지네이션이 끝나지 않았다. DART 는 `total_page` 를 넘겨도
  오류 없이 **마지막 페이지를 계속 돌려준다**(page 50 과 200 의 첫 행이 동일).
  'rows<100 이면 끝' 조건이 영원히 거짓 → 같은 행을 중복으로 쌓으며 호출만 태웠다.
  결과는 중복 제거 덕에 틀리지 않았지만 호출 낭비가 컸다. `total_page` 기준으로 교체.
- **버그 12**: `pct_change` 의 기본 `fill_method='ffill'`. 결측을 앞값으로 채운 뒤
  증가율을 계산하면 매출이 없던 해를 전년과 같다고 치고 증가율 0%를 만든다. → 껐다.

### 예측 창을 400일로 올림 (설계 변경)

| | |
|---|---|
| 제출간격 중앙값 | **364일** — 12개월 창이 분포 한가운데를 자른다 |
| 창 365일 초과 | 8,401건 (33.7%) |
| 라벨 사건 365/400/450일 | 359 / **532** / 562건 |
| 창 초과율: 제출기한 연장 없음 vs 있음 | 33.2% vs **79.8%** (2.4배) |

A-2 가설 확인. 12개월 창이 지우는 사건은 무작위가 아니라 **감사가 제때 끝나지 않은
기업**, 곧 부실 신호가 켜진 기업에 몰려 있다. 가장 중요한 사건부터 지워진다.
→ `HORIZON_DAYS = 400`. 365·450 은 민감도로 병행. 면담 안건 6번으로 추가.

> 진단 코드를 처음엔 거꾸로 짰다. 간격을 늘리는 것은 **뒤쪽 보고서가 늦는 것**인데
> 연장 플래그를 앞 연도에 붙여서 13.3% vs 33.9% 로 반대 결론이 나왔다.
> 정렬을 고치니 79.8% vs 33.2%. 방향을 한 칸 어긋나게 짜면 결론이 뒤집힌다.

### 남은 우려
- **검증 구간(2023) 사건이 34건**뿐이다(창 365일 기준). 하이퍼파라미터 조정에 쓰기엔 적다.
  창 400일로 올리면 늘지만 여전히 적다. 7~8주차에 검증 구간을 2022~2023 으로 넓힐지 판단.

## 2026-09-04 (오전) — 데이터셋 확정 · 기준 성능

`build` → `train` 완주. **수치는 전부 `docs/10_실측결과.md` 에 누적한다.**

- 데이터셋 확정: 관측치 23,084 / 사건 532 (2.30%, 창 400일).
- 기준 성능(평가 2024~25, 사건비율 3.12%):
  로지스틱 PR-AUC 0.216 (기준선 6.9배) / 앙상블 0.249 (8.0배). ROC-AUC 0.87~0.88.
  **두 신뢰구간이 크게 겹쳐 우열을 말하지 않는다.**
- **버그 13**: `event_type` 이 창과 무관하게 채워졌다. y=0 인 1,792행에도 유형이 붙어
  '이 기업에 언젠가 사건이 있었다'를 뜻하고 있었다. 12주차 오분류 분석에서
  `event_type=='회생절차'` 로 거르면 사건 아닌 행이 딸려온다. → 창 안 사건만 남기도록 마스킹.
- **버그 14**: 윈저라이즈가 `완전자본잠식` 을 지웠다. 발생률 0.53%라 99분위가 0 이고,
  클리핑하면 전 행이 0 이 된다. **자본총계가 음수라는 부실의 가장 직접적 신호가
  희소하다는 이유로 제거됐다.** 이진 변수를 클리핑에서 제외.
  되살리자 로지스틱 재현율@정밀도0.3 이 0.013 → 0.34 로 뛰었다.

### 내가 틀렸던 것 하나

로지스틱 표준화 계수에서 결측 더미가 상위를 독식하길래 "모델이 재무상태가 아니라
결측으로 판단한다"고 판단할 뻔했다. 절제 실험으로 확인하니 아니었다.

| 변수 집합 | 평가 PR-AUC |
|---|---|
| 재무변수 + 결측더미 | 0.2485 |
| 재무변수만 | 0.2377 |
| 결측더미만 | 0.0343 (기준선 0.0312) |

0/1 더미는 표준화하면 분산이 작아 계수가 부풀려진다. **표준화 계수 크기를
변수 중요도로 읽지 않는다.** 인상적인 서사일수록 절제 실험으로 확인하고 쓴다.

## 2026-09-04 (오후) — 확률 보정 완료 · 프로토콜 위반 신고

- **확률 보정 (미해결 1번 종결)**: isotonic. Brier 0.0853 → **0.0209**,
  예측확률 평균 0.2048 → 0.0250 (검증 구간 실제 0.0268).
  보정 전이면 설명 카드에 "부실확률 37%" 로 찍힐 사례가 실제 4% 수준이었다.
  카드 확률은 보정 모형에서, SHAP 은 보정 전 모형에서 뽑는다(단조변환이라 방향 불변).
  판단은 **검증 구간에서만** 했다. → `docs/10_실측결과.md` §3-2
- **프로토콜 위반 신고**: W08 의 "평가 구간 1회 개봉" 규칙을 어기고 3회 열었다.
  모델 선택에 쓰지는 않았으나 사실은 사실이다. 개봉 기록표를 §3-3 에 남기고
  최종보고서에 그대로 싣는다. 결측 더미 절제 실험은 검증 구간에서 재실행했다.
  앞으로 모든 진단은 검증 구간에서만, 평가 구간은 최종 1회.

## 〔다음 기록〕
- [ ] **DART API 키 발급** ← 여기서 막혀 있음. 키가 있어야 수집이 시작된다
- [ ] 지도교수 1차 면담 → 정의 문서 v1.0 승격 + 범위 변경 보고
